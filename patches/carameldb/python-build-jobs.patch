--- pybind/setup.py
+++ pybind/setup.py
@@ -100,4 +100,6 @@
-        jobs = multiprocessing.cpu_count() * 2
+        jobs = int(
+            os.environ.get("CARAMEL_BUILD_JOBS", multiprocessing.cpu_count() * 2)
+        )
         build_args += [f"-j{jobs}"]
 
         build_dir = "build/"
